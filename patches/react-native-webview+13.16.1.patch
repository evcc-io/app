diff --git a/node_modules/react-native-webview/android/src/main/java/com/reactnativecommunity/webview/RNCWebViewClient.java b/node_modules/react-native-webview/android/src/main/java/com/reactnativecommunity/webview/RNCWebViewClient.java
index 0287e1c..7835002 100644
--- a/node_modules/react-native-webview/android/src/main/java/com/reactnativecommunity/webview/RNCWebViewClient.java
+++ b/node_modules/react-native-webview/android/src/main/java/com/reactnativecommunity/webview/RNCWebViewClient.java
@@ -10,6 +10,7 @@ import android.webkit.HttpAuthHandler;
 import android.webkit.RenderProcessGoneDetail;
 import android.webkit.SslErrorHandler;
 import android.webkit.WebResourceRequest;
+import android.webkit.ClientCertRequest;
 import android.webkit.WebResourceResponse;
 import android.webkit.WebView;
 import android.webkit.WebViewClient;
@@ -327,4 +328,22 @@ public class RNCWebViewClient extends WebViewClient {
     public void setProgressChangedFilter(RNCWebView.ProgressChangedFilter filter) {
         progressChangedFilter = filter;
     }
+
+  @Override
+  public void onReceivedClientCertRequest(WebView view, ClientCertRequest request) {
+    try {
+        Class<?> clazz = Class.forName("expo.modules.clientcert.ClientCertStore");
+        java.security.PrivateKey key = (java.security.PrivateKey) clazz.getMethod("getPrivateKey").invoke(null);
+        java.security.cert.X509Certificate[] chain = (java.security.cert.X509Certificate[]) clazz.getMethod("getCertificateChain").invoke(null);
+        
+        if (key != null && chain != null) {
+            request.proceed(key, chain);
+            return;
+        }
+    } catch (Exception e) {
+        android.util.Log.e("ClientCertPatch", "Error loading client cert", e);
+    }
+    super.onReceivedClientCertRequest(view, request);
+  }
+
 }
